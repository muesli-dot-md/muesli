target/
node_modules/
dist/
.DS_Store
*.onnx
# ...except the small Silero VAD model the vendored crate include_bytes!()es at
# compile time (the large Parakeet STT model is downloaded at runtime).
!vendor/voice_activity_detector/src/silero_vad.onnx
models/

# SvelteKit
/build
/.svelte-kit
/package

# Env files
.env
.env.*
!.env.example

# Vite temp files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Agent worktrees (scratch)
.claude/worktrees/
